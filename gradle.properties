# Disable parallel execution to prevent multiple compiler daemons from consuming too much memory
org.gradle.parallel=false

# Allocate sufficient memory for Gradle and Kotlin compilers
org.gradle.jvmargs=-Xmx2048m -XX:MaxMetaspaceSize=512m
kotlin.daemon.jvmargs=-Xmx1536m
